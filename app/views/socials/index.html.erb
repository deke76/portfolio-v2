<%= turbo_frame_tag "turbo-page-content" do %>
  
  <%= render "layouts/navigation/page_header" %>
  
  <div class="container-fluid d-flex flex-column justify-content-between h-100">
    <div class="container-fluid flex-grow-1" >

      <%= turbo_frame_tag "turbo-social-content", class: "h-100 d-flex flex-column" do %>
      <% end %>

    </div>
  </div>
<% end %>